defaults:
  - physics: phys #physical constants
  - exp_type: TL_nonisothermal #experiment_type
  - setup: setup #manual setup such as use A_isoth or self calculate based on other parameters

hydra:
  job:
    chdir: true